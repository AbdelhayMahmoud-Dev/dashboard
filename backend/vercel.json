{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "buildCommand": "echo 'No build step: the Express app is bundled per-function by @vercel/node from api/index.ts'",
  "rewrites": [{ "source": "/(.*)", "destination": "/api" }]
}
