diff --git a/node_modules/next/dist/build/utils.js b/node_modules/next/dist/build/utils.js
index 82b6eb4..bfcfdf2 100644
--- a/node_modules/next/dist/build/utils.js
+++ b/node_modules/next/dist/build/utils.js
@@ -568,8 +568,10 @@ function printCustomRoutes({ redirects, rewrites, headers, onMatchHeaders }) {
 async function isPageStatic({ dir, page, distDir, configFileName, httpAgentOptions, locales, defaultLocale, parentId, pageRuntime, edgeInfo, pageType, cacheComponents, authInterrupts, originalAppPath, isrFlushToDisk, cacheMaxMemorySize, nextConfigOutput, cacheHandler, cacheHandlers, cacheLifeProfiles, pprConfig, partialFallbacksEnabled, buildId, deploymentId, clientAssetToken, sriEnabled }) {
     // Skip page data collection for synthetic _global-error routes
     if (page === _constants1.UNDERSCORE_GLOBAL_ERROR_ROUTE) {
+        // PATCHED: force-dynamic so the route is skipped from staticPaths
+        // and the prerender phase never runs (avoids workStore invariant bug).
         return {
-            isStatic: true,
+            isStatic: false,
             isRoutePPREnabled: false,
             prerenderFallbackMode: undefined,
             prerenderedRoutes: undefined,
@@ -577,7 +579,7 @@ async function isPageStatic({ dir, page, distDir, configFileName, httpAgentOptio
             hasStaticProps: false,
             hasServerProps: false,
             isNextImageImported: false,
-            appConfig: {}
+            appConfig: { dynamic: 'force-dynamic', revalidate: 0 }
         };
     }
     await (0, _createincrementalcache.createIncrementalCache)({
diff --git a/node_modules/next/dist/esm/build/utils.js b/node_modules/next/dist/esm/build/utils.js
index 50cda1b..6ab40e4 100644
--- a/node_modules/next/dist/esm/build/utils.js
+++ b/node_modules/next/dist/esm/build/utils.js
@@ -379,8 +379,10 @@ export function printCustomRoutes({ redirects, rewrites, headers, onMatchHeaders
 export async function isPageStatic({ dir, page, distDir, configFileName, httpAgentOptions, locales, defaultLocale, parentId, pageRuntime, edgeInfo, pageType, cacheComponents, authInterrupts, originalAppPath, isrFlushToDisk, cacheMaxMemorySize, nextConfigOutput, cacheHandler, cacheHandlers, cacheLifeProfiles, pprConfig, partialFallbacksEnabled, buildId, deploymentId, clientAssetToken, sriEnabled }) {
     // Skip page data collection for synthetic _global-error routes
     if (page === UNDERSCORE_GLOBAL_ERROR_ROUTE) {
+        // PATCHED: force-dynamic so the route is skipped from staticPaths
+        // and the prerender phase never runs (avoids workStore invariant bug).
         return {
-            isStatic: true,
+            isStatic: false,
             isRoutePPREnabled: false,
             prerenderFallbackMode: undefined,
             prerenderedRoutes: undefined,
@@ -388,7 +390,7 @@ export async function isPageStatic({ dir, page, distDir, configFileName, httpAge
             hasStaticProps: false,
             hasServerProps: false,
             isNextImageImported: false,
-            appConfig: {}
+            appConfig: { dynamic: 'force-dynamic', revalidate: 0 }
         };
     }
     await createIncrementalCache({
diff --git a/node_modules/next/dist/esm/export/routes/app-page.js b/node_modules/next/dist/esm/export/routes/app-page.js
index d454fbc..ed0d8c9 100644
--- a/node_modules/next/dist/esm/export/routes/app-page.js
+++ b/node_modules/next/dist/esm/export/routes/app-page.js
@@ -29,6 +29,10 @@ import { UNDERSCORE_GLOBAL_ERROR_ROUTE_ENTRY, UNDERSCORE_NOT_FOUND_ROUTE_ENTRY }
     if (page === UNDERSCORE_GLOBAL_ERROR_ROUTE_ENTRY) {
         isDefaultGlobalError = true;
         pathname = '/500';
+        // PATCHED (Next.js 16.2.6 workStore bug): lazyRenderAppPage fails for
+        // this route because workStore is not initialised during static
+        // generation. Return dynamic (revalidate:0) to skip static prerender.
+        return { cacheControl: { revalidate: 0, expire: undefined } };
     }
     try {
         const result = await lazyRenderAppPage(new NodeNextRequest(req), new NodeNextResponse(res), pathname, query, fallbackRouteParams, renderOpts, undefined, sharedContext);
diff --git a/node_modules/next/dist/export/routes/app-page.js b/node_modules/next/dist/export/routes/app-page.js
index 077bd53..ac98add 100644
--- a/node_modules/next/dist/export/routes/app-page.js
+++ b/node_modules/next/dist/export/routes/app-page.js
@@ -37,6 +37,10 @@ async function exportAppPage(req, res, page, path, pathname, query, fallbackRout
     if (page === _entryconstants.UNDERSCORE_GLOBAL_ERROR_ROUTE_ENTRY) {
         isDefaultGlobalError = true;
         pathname = '/500';
+        // PATCHED (Next.js 16.2.6 workStore bug): lazyRenderAppPage fails for
+        // this route because workStore is not initialised during static
+        // generation. Return dynamic (revalidate:0) to skip static prerender.
+        return { cacheControl: { revalidate: 0, expire: undefined } };
     }
     try {
         const result = await (0, _modulerender.lazyRenderAppPage)(new _node.NodeNextRequest(req), new _node.NodeNextResponse(res), pathname, query, fallbackRouteParams, renderOpts, undefined, sharedContext);
